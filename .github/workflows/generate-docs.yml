name: Generate API Documentation

on:
  push:
    branches:
      - main
      - auto-docs

jobs:
  build-docs:
    runs-on: ubuntu-latest

    steps:
    - name: publish
      uses: frankhaugen/frank-docfx-publish@1.0
